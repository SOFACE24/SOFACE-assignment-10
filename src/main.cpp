#include "snippets.hpp"
#include <iostream>

int main()
{

    SortedVector<int> v;

    // v.insert(1);
    // v.insert(2);
    // v.insert(3);

    // v.contains(4);

    std::cout << "array contains the value: '" << v.contains(2) << "'" << std::endl;
}